
chrome.commands.onCommand.addListener((command) => {
  if (command === 'recursive-search') {
    // 1. open a pop-up window with a form
    // 2. run the search
    // 3. render results
    // prototype: default search for 50%
  } else {
    throw 42;
  }
});
